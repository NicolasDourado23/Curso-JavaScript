 const idade = 28;
 const nome = 'Nicolas';

// if é igual a "se"

// Quando se usar "||" e so um estiver verdadeiro passa, mas se os dois estiverem errados não passa.

// Quando se usar "&&" e um dos dois estiverem errados não entra.

//"=" Atribuição, "==" Contribuição, "===" Comparativo preciso.

 if (idade >= 18 && nome === 'Nicolas'){
    console.log('ENTROU!!!');
} 
 //e else é igual a "senao"

 else {
    console.log('NAO ENTRA!!!')
 }

